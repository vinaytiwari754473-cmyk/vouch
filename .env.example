# The committed demo defaults to replay and needs no secrets.
VOUCH_AI_MODE=replay

# Live capture is optional development tooling. Freeze one provider/model before evaluation.
# OPENAI_API_KEY=
# VOUCH_MODEL_PROVIDER=openai
# VOUCH_MODEL_ID=
# VOUCH_PROMPT_VERSION=vouch-investigator/1
